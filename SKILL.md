---
name: feature-study-workflow
description: Use for learning-centered development of a new or substantially changed feature from requirements through implementation. The user proposes requirements, behavior flows, and a high-level construction roadmap before AI review; then one observable flow is built, validated, audited, and understood at a time. Do not use for primarily studying an existing codebase without a feature goal; use codebase-study-workflow instead.
---

# Feature Study Workflow Compatibility Entry

This repository is also a plugin containing two skills. This root entry keeps an existing standalone installation of `$feature-study-workflow` working.

Before doing the task, read `skills/feature-study-workflow/SKILL.md` completely and follow it as the authoritative workflow. Resolve every relative reference from `skills/feature-study-workflow/`.
